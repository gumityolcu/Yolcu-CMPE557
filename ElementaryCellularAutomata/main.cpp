#include <iostream>
#include <vector>

using namespace std;

vector<vector<bool>> RunElementaryCellularAutomaton(char W, vector<bool> init);

int main()
{
    cout<<sizeof(char);
    return 0;
}

vector<vector<bool>> RunElementaryCellularAutomaton(char W, vector<bool> init)
{

}
